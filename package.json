{
  "name": "stewarthaines.com",
  "version": "1.0.0",
  "description": ".",
  "main": "index.js",
  "scripts": {
    "build": "eleventy",
    "dev": "eleventy --serve --watch",
    "dev-https": "npm run dev & npx local-ssl-proxy --source 8443 --target 8080 --hostname ibis.local",
    "start": "eleventy --serve",
    "clean": "rm -rf _site",
    "extract-i18n": "node scripts/extract-i18n.js",
    "update-po": "bash scripts/update-po-files.sh",
    "po-to-json": "node scripts/po-to-json.js",
    "i18n-workflow": "npm run extract-i18n && npm run update-po && npm run po-to-json"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@11ty/eleventy": "^3.1.2",
    "gettext-extractor": "^4.0.1",
    "glob": "^11.0.3",
    "local-ssl-proxy": "^2.0.5",
    "markdown-it": "^14.1.0"
  },
  "dependencies": {
    "@xmldom/xmldom": "^0.8.11",
    "eleventy-plugin-i18n": "^0.1.3",
    "unzipper": "^0.12.3"
  }
}
